# 当加载二分类预训练权重 Swin-MS4-bi-seed42-s-crop/checkpoint_best_regular.pth 时

1. 打开datasets/metrics.py文件，到第272行：

```python
# tusz二分类的
# selected_thresholds_index = np.array([[26,26,26],[27,27,27],[22,22,22]])
```

2. 将该注释去除：

```python
# tusz二分类的
selected_thresholds_index = np.array([[26,26,26],[27,27,27],[22,22,22]])
```

# 当加载多分类预训练权重 Swin-MS4-bi-seed42-sns-crop-ft/checkpoint_best_regular.pth 时

1. 打开datasets/metrics.py文件，到第274行：

```python
# tusz多分类的
# selected_thresholds_index = np.array([[24, 25, 24], [26, 23, 26], [22, 23, 22]])
```

2. 将该注释去除：

```python
# tusz多分类的
selected_thresholds_index = np.array([[24, 25, 24], [26, 23, 26], [22, 23, 22]])
```

# 此操作缘于历史遗留，以后重新训练的模型，不需要此操作

# 两个预训练权重的num_classes都为5