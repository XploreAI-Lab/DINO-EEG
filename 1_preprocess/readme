get_GT_from_bids.py
get_annotations_from_bids.py
用于从 BIDS 格式数据集中生成后续评估需要的 GT 和 annotation 文件。
适用于 CHBMIT、Siena、TUSZ 等 BIDS 格式数据集。

get_3label_from_0_unused.py
将原始标签中的 `0` 调整为 `5`，并仅保留 label 信息。
- 更像是一次标签重编码的预处理：把背景类从 `0` 改到 `5`
- 该脚本不会保留 signal，因此不适合依赖原始信号的后续流程

get_slice_signal_and_label.py
将长时程 EEG / STFT 信号文件及对应标签切分成固定长度的 slice，供后续训练或评估使用。
- 读取原始 HDF5 信号文件（如 `stft`）和标签文件（如 `bilabel`）
- 通过滑动窗口机制切分长信号，默认窗口约 200 秒、50% 重叠
- 根据标签判断每个切片是否包含 seizure 事件
- 输出切片后的 `.h5` 文件，并生成对应的 `.txt` 索引文件

get_annotations_from_h5.py
从 H5 数据中生成 annotation 文件。

标签约定
- 正常或非癫痫片段记为 `5`
- 癫痫事件记为 `1`

采样率备注
- Siena 预处理使用 20 Hz 滤波
- TUSZ 预处理使用 20 Hz

get_stfth5_from_h5_2.py
相比早期版本，这个脚本更工程化，适合作为当前主流程的 STFT 转换实现。

pipe_EDF_to_result_unused.py
旧版端到端脚本，当前不建议使用。

checkpoint example
autodl-tmp/dinolbh/logs0624/DINO/TUSZ/090816_two_stage/checkpoint_best_regular.pth
